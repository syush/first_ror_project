<%= form_for([:admin, @category]) do |f| %>

  <%= render 'error/handle', :errors => @category.errors  %>
  <div class="field">
    <p><strong>Введите название категории:</strong></p>
    <%= f.text_area :title %>
  </div>
  <div class="actions">
    <%= f.submit "Сохранить" %>
  </div>
<% end %>
